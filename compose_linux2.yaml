services:
  client:
    build: ./client
    environment:
      - INTERACTIVE=false
      - SERVER_URL=ws://192.168.200.12:8080
    network_mode: host
    deploy:
      mode: replicated
      replicas: 3
    restart: always

networks:
  default:
    driver: bridge
